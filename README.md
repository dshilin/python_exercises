# 🐍 Python Exercises — подготовка к live coding (QA Automation)

Материалы для подготовки к секции **live coding** на собеседовании на вакансию **QA Automation на Python**.

---

## 📁 Структура проекта

| Файл | Описание |
|------|----------|
| `python_lesson_1.ipynb` | Базовый Python: ввод данных, типы, списки, словари, циклы |
| `python_lesson_2.ipynb` | Множества (`set`): пересечение/объединение/разность, поиск общих и уникальных элементов |
| `python_lesson_3.ipynb` | Функциональное программирование: `map()`, `lambda`, `zip()`, list comprehension |
| `python_lesson_4_lr.ipynb` | Линейная регрессия: вычисление коэффициентов m и b, остатки, numpy |

---

## 🎯 Что отрабатывается

- **Типы и ввод:** `input()`, приведение типов (`int()`, `str()`), аннотации типов
- **Списки и циклы:** создание списков, `append()`, цикл `for`, `range()`
- **Словари:** создание, доступ по ключу, добавление и изменение полей
- **Функции:** параметры, возврат значений (в т.ч. кортежа), docstring
- **Множества:** `set`, пересечение/объединение/разность (`&`, `|`, `-`), `set.intersection(*values)`, `set.union(*values)`; задачи на общие/уникальные элементы
- **Функциональное программирование:** `map()`, `lambda` функции, `zip()` для работы с несколькими последовательностями, list comprehension
- **Линейная регрессия:** вычисление коэффициентов линейной регрессии (m и b), работа с numpy для векторизованных вычислений, расчёт остатков

---

## 🚀 Как запускать

1. Клонируйте репозиторий и перейдите в папку проекта.
2. Установите Jupyter (если ещё не установлен):
   ```bash
   pip install jupyter
   ```
3. Запустите Jupyter:
   ```bash
   jupyter notebook
   ```
4. Откройте нужный `.ipynb` и выполняйте ячейки по порядку.

---

## 💡 Советы для live coding

- Пишите код по шагам: сначала простая версия, потом усложнение.
- Проговорите условие задачи и план решения перед кодом.
- Проверяйте краевые случаи (пустой список, один элемент).
- Используйте понятные имена переменных и короткие функции.

Удачи на собеседовании! 🍀
